# express-backend-starter
A simple Express backend starter written in TypeScript. Includes a scalable folder structure, healthcheck route, database connection setup, and common utility helpers like ApiResponse, ApiError, global error handler, and asyncHandler. Designed as a minimal, logic-free foundation that can be extended for projects.
